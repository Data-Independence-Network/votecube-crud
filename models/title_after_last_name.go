// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// TitleAfterLastName is an object representing the database table.
type TitleAfterLastName struct {
	TitleAfterLastNameID int64  `boil:"title_after_last_name_id" json:"title_after_last_name_id" toml:"title_after_last_name_id" yaml:"title_after_last_name_id"`
	TitleAfterLastName   string `boil:"title_after_last_name" json:"title_after_last_name" toml:"title_after_last_name" yaml:"title_after_last_name"`

	R *titleAfterLastNameR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L titleAfterLastNameL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TitleAfterLastNameColumns = struct {
	TitleAfterLastNameID string
	TitleAfterLastName   string
}{
	TitleAfterLastNameID: "title_after_last_name_id",
	TitleAfterLastName:   "title_after_last_name",
}

// TitleAfterLastNameRels is where relationship names are stored.
var TitleAfterLastNameRels = struct {
	UserPersonalInfoTitleAfterLastNames string
}{
	UserPersonalInfoTitleAfterLastNames: "UserPersonalInfoTitleAfterLastNames",
}

// titleAfterLastNameR is where relationships are stored.
type titleAfterLastNameR struct {
	UserPersonalInfoTitleAfterLastNames UserPersonalInfoTitleAfterLastNameSlice
}

// NewStruct creates a new relationship struct
func (*titleAfterLastNameR) NewStruct() *titleAfterLastNameR {
	return &titleAfterLastNameR{}
}

// titleAfterLastNameL is where Load methods for each relationship are stored.
type titleAfterLastNameL struct{}

var (
	titleAfterLastNameColumns               = []string{"title_after_last_name_id", "title_after_last_name"}
	titleAfterLastNameColumnsWithoutDefault = []string{"title_after_last_name_id", "title_after_last_name"}
	titleAfterLastNameColumnsWithDefault    = []string{}
	titleAfterLastNamePrimaryKeyColumns     = []string{"title_after_last_name_id"}
)

type (
	// TitleAfterLastNameSlice is an alias for a slice of pointers to TitleAfterLastName.
	// This should generally be used opposed to []TitleAfterLastName.
	TitleAfterLastNameSlice []*TitleAfterLastName
	// TitleAfterLastNameHook is the signature for custom TitleAfterLastName hook methods
	TitleAfterLastNameHook func(context.Context, boil.ContextExecutor, *TitleAfterLastName) error

	titleAfterLastNameQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	titleAfterLastNameType                 = reflect.TypeOf(&TitleAfterLastName{})
	titleAfterLastNameMapping              = queries.MakeStructMapping(titleAfterLastNameType)
	titleAfterLastNamePrimaryKeyMapping, _ = queries.BindMapping(titleAfterLastNameType, titleAfterLastNameMapping, titleAfterLastNamePrimaryKeyColumns)
	titleAfterLastNameInsertCacheMut       sync.RWMutex
	titleAfterLastNameInsertCache          = make(map[string]insertCache)
	titleAfterLastNameUpdateCacheMut       sync.RWMutex
	titleAfterLastNameUpdateCache          = make(map[string]updateCache)
	titleAfterLastNameUpsertCacheMut       sync.RWMutex
	titleAfterLastNameUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
)

var titleAfterLastNameBeforeInsertHooks []TitleAfterLastNameHook
var titleAfterLastNameBeforeUpdateHooks []TitleAfterLastNameHook
var titleAfterLastNameBeforeDeleteHooks []TitleAfterLastNameHook
var titleAfterLastNameBeforeUpsertHooks []TitleAfterLastNameHook

var titleAfterLastNameAfterInsertHooks []TitleAfterLastNameHook
var titleAfterLastNameAfterSelectHooks []TitleAfterLastNameHook
var titleAfterLastNameAfterUpdateHooks []TitleAfterLastNameHook
var titleAfterLastNameAfterDeleteHooks []TitleAfterLastNameHook
var titleAfterLastNameAfterUpsertHooks []TitleAfterLastNameHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *TitleAfterLastName) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range titleAfterLastNameBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *TitleAfterLastName) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range titleAfterLastNameBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *TitleAfterLastName) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range titleAfterLastNameBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *TitleAfterLastName) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range titleAfterLastNameBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *TitleAfterLastName) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range titleAfterLastNameAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *TitleAfterLastName) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range titleAfterLastNameAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *TitleAfterLastName) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range titleAfterLastNameAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *TitleAfterLastName) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range titleAfterLastNameAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *TitleAfterLastName) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range titleAfterLastNameAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTitleAfterLastNameHook registers your hook function for all future operations.
func AddTitleAfterLastNameHook(hookPoint boil.HookPoint, titleAfterLastNameHook TitleAfterLastNameHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		titleAfterLastNameBeforeInsertHooks = append(titleAfterLastNameBeforeInsertHooks, titleAfterLastNameHook)
	case boil.BeforeUpdateHook:
		titleAfterLastNameBeforeUpdateHooks = append(titleAfterLastNameBeforeUpdateHooks, titleAfterLastNameHook)
	case boil.BeforeDeleteHook:
		titleAfterLastNameBeforeDeleteHooks = append(titleAfterLastNameBeforeDeleteHooks, titleAfterLastNameHook)
	case boil.BeforeUpsertHook:
		titleAfterLastNameBeforeUpsertHooks = append(titleAfterLastNameBeforeUpsertHooks, titleAfterLastNameHook)
	case boil.AfterInsertHook:
		titleAfterLastNameAfterInsertHooks = append(titleAfterLastNameAfterInsertHooks, titleAfterLastNameHook)
	case boil.AfterSelectHook:
		titleAfterLastNameAfterSelectHooks = append(titleAfterLastNameAfterSelectHooks, titleAfterLastNameHook)
	case boil.AfterUpdateHook:
		titleAfterLastNameAfterUpdateHooks = append(titleAfterLastNameAfterUpdateHooks, titleAfterLastNameHook)
	case boil.AfterDeleteHook:
		titleAfterLastNameAfterDeleteHooks = append(titleAfterLastNameAfterDeleteHooks, titleAfterLastNameHook)
	case boil.AfterUpsertHook:
		titleAfterLastNameAfterUpsertHooks = append(titleAfterLastNameAfterUpsertHooks, titleAfterLastNameHook)
	}
}

// One returns a single titleAfterLastName record from the query.
func (q titleAfterLastNameQuery) One(ctx context.Context, exec boil.ContextExecutor) (*TitleAfterLastName, error) {
	o := &TitleAfterLastName{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for title_after_last_name")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all TitleAfterLastName records from the query.
func (q titleAfterLastNameQuery) All(ctx context.Context, exec boil.ContextExecutor) (TitleAfterLastNameSlice, error) {
	var o []*TitleAfterLastName

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to TitleAfterLastName slice")
	}

	if len(titleAfterLastNameAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all TitleAfterLastName records in the query.
func (q titleAfterLastNameQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count title_after_last_name rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q titleAfterLastNameQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if title_after_last_name exists")
	}

	return count > 0, nil
}

// UserPersonalInfoTitleAfterLastNames retrieves all the user_personal_info_title_after_last_name's UserPersonalInfoTitleAfterLastNames with an executor.
func (o *TitleAfterLastName) UserPersonalInfoTitleAfterLastNames(mods ...qm.QueryMod) userPersonalInfoTitleAfterLastNameQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"user_personal_info_title_after_last_name\".\"title_after_last_name_id\"=?", o.TitleAfterLastNameID),
	)

	query := UserPersonalInfoTitleAfterLastNames(queryMods...)
	queries.SetFrom(query.Query, "\"user_personal_info_title_after_last_name\"")

	if len(queries.GetSelect(query.Query)) == 0 {
		queries.SetSelect(query.Query, []string{"\"user_personal_info_title_after_last_name\".*"})
	}

	return query
}

// LoadUserPersonalInfoTitleAfterLastNames allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (titleAfterLastNameL) LoadUserPersonalInfoTitleAfterLastNames(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTitleAfterLastName interface{}, mods queries.Applicator) error {
	var slice []*TitleAfterLastName
	var object *TitleAfterLastName

	if singular {
		object = maybeTitleAfterLastName.(*TitleAfterLastName)
	} else {
		slice = *maybeTitleAfterLastName.(*[]*TitleAfterLastName)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &titleAfterLastNameR{}
		}
		args = append(args, object.TitleAfterLastNameID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &titleAfterLastNameR{}
			}

			for _, a := range args {
				if a == obj.TitleAfterLastNameID {
					continue Outer
				}
			}

			args = append(args, obj.TitleAfterLastNameID)
		}
	}

	query := NewQuery(qm.From(`user_personal_info_title_after_last_name`), qm.WhereIn(`title_after_last_name_id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load user_personal_info_title_after_last_name")
	}

	var resultSlice []*UserPersonalInfoTitleAfterLastName
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice user_personal_info_title_after_last_name")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on user_personal_info_title_after_last_name")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for user_personal_info_title_after_last_name")
	}

	if len(userPersonalInfoTitleAfterLastNameAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.UserPersonalInfoTitleAfterLastNames = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &userPersonalInfoTitleAfterLastNameR{}
			}
			foreign.R.TitleAfterLastName = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.TitleAfterLastNameID == foreign.TitleAfterLastNameID {
				local.R.UserPersonalInfoTitleAfterLastNames = append(local.R.UserPersonalInfoTitleAfterLastNames, foreign)
				if foreign.R == nil {
					foreign.R = &userPersonalInfoTitleAfterLastNameR{}
				}
				foreign.R.TitleAfterLastName = local
				break
			}
		}
	}

	return nil
}

// AddUserPersonalInfoTitleAfterLastNames adds the given related objects to the existing relationships
// of the title_after_last_name, optionally inserting them as new records.
// Appends related to o.R.UserPersonalInfoTitleAfterLastNames.
// Sets related.R.TitleAfterLastName appropriately.
func (o *TitleAfterLastName) AddUserPersonalInfoTitleAfterLastNames(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*UserPersonalInfoTitleAfterLastName) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.TitleAfterLastNameID = o.TitleAfterLastNameID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"user_personal_info_title_after_last_name\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"title_after_last_name_id"}),
				strmangle.WhereClause("\"", "\"", 2, userPersonalInfoTitleAfterLastNamePrimaryKeyColumns),
			)
			values := []interface{}{o.TitleAfterLastNameID, rel.UserPersonalInfoTitleAfterLastNameID}

			if boil.DebugMode {
				fmt.Fprintln(boil.DebugWriter, updateQuery)
				fmt.Fprintln(boil.DebugWriter, values)
			}

			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.TitleAfterLastNameID = o.TitleAfterLastNameID
		}
	}

	if o.R == nil {
		o.R = &titleAfterLastNameR{
			UserPersonalInfoTitleAfterLastNames: related,
		}
	} else {
		o.R.UserPersonalInfoTitleAfterLastNames = append(o.R.UserPersonalInfoTitleAfterLastNames, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &userPersonalInfoTitleAfterLastNameR{
				TitleAfterLastName: o,
			}
		} else {
			rel.R.TitleAfterLastName = o
		}
	}
	return nil
}

// TitleAfterLastNames retrieves all the records using an executor.
func TitleAfterLastNames(mods ...qm.QueryMod) titleAfterLastNameQuery {
	mods = append(mods, qm.From("\"title_after_last_name\""))
	return titleAfterLastNameQuery{NewQuery(mods...)}
}

// FindTitleAfterLastName retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTitleAfterLastName(ctx context.Context, exec boil.ContextExecutor, titleAfterLastNameID int64, selectCols ...string) (*TitleAfterLastName, error) {
	titleAfterLastNameObj := &TitleAfterLastName{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"title_after_last_name\" where \"title_after_last_name_id\"=$1", sel,
	)

	q := queries.Raw(query, titleAfterLastNameID)

	err := q.Bind(ctx, exec, titleAfterLastNameObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from title_after_last_name")
	}

	return titleAfterLastNameObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TitleAfterLastName) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no title_after_last_name provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(titleAfterLastNameColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	titleAfterLastNameInsertCacheMut.RLock()
	cache, cached := titleAfterLastNameInsertCache[key]
	titleAfterLastNameInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			titleAfterLastNameColumns,
			titleAfterLastNameColumnsWithDefault,
			titleAfterLastNameColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(titleAfterLastNameType, titleAfterLastNameMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(titleAfterLastNameType, titleAfterLastNameMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"title_after_last_name\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"title_after_last_name\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into title_after_last_name")
	}

	if !cached {
		titleAfterLastNameInsertCacheMut.Lock()
		titleAfterLastNameInsertCache[key] = cache
		titleAfterLastNameInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the TitleAfterLastName.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TitleAfterLastName) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	titleAfterLastNameUpdateCacheMut.RLock()
	cache, cached := titleAfterLastNameUpdateCache[key]
	titleAfterLastNameUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			titleAfterLastNameColumns,
			titleAfterLastNamePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update title_after_last_name, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"title_after_last_name\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, titleAfterLastNamePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(titleAfterLastNameType, titleAfterLastNameMapping, append(wl, titleAfterLastNamePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update title_after_last_name row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for title_after_last_name")
	}

	if !cached {
		titleAfterLastNameUpdateCacheMut.Lock()
		titleAfterLastNameUpdateCache[key] = cache
		titleAfterLastNameUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q titleAfterLastNameQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for title_after_last_name")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for title_after_last_name")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TitleAfterLastNameSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), titleAfterLastNamePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"title_after_last_name\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, titleAfterLastNamePrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in titleAfterLastName slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all titleAfterLastName")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TitleAfterLastName) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no title_after_last_name provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(titleAfterLastNameColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	titleAfterLastNameUpsertCacheMut.RLock()
	cache, cached := titleAfterLastNameUpsertCache[key]
	titleAfterLastNameUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			titleAfterLastNameColumns,
			titleAfterLastNameColumnsWithDefault,
			titleAfterLastNameColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			titleAfterLastNameColumns,
			titleAfterLastNamePrimaryKeyColumns,
		)

		if len(update) == 0 {
			return errors.New("models: unable to upsert title_after_last_name, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(titleAfterLastNamePrimaryKeyColumns))
			copy(conflict, titleAfterLastNamePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryCockroachDB(dialect, "\"title_after_last_name\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(titleAfterLastNameType, titleAfterLastNameMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(titleAfterLastNameType, titleAfterLastNameMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		_, _ = fmt.Fprintln(boil.DebugWriter, cache.query)
		_, _ = fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // CockcorachDB doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert title_after_last_name")
	}

	if !cached {
		titleAfterLastNameUpsertCacheMut.Lock()
		titleAfterLastNameUpsertCache[key] = cache
		titleAfterLastNameUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single TitleAfterLastName record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TitleAfterLastName) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no TitleAfterLastName provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), titleAfterLastNamePrimaryKeyMapping)
	sql := "DELETE FROM \"title_after_last_name\" WHERE \"title_after_last_name_id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from title_after_last_name")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for title_after_last_name")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q titleAfterLastNameQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no titleAfterLastNameQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from title_after_last_name")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for title_after_last_name")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TitleAfterLastNameSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no TitleAfterLastName slice provided for delete all")
	}

	if len(o) == 0 {
		return 0, nil
	}

	if len(titleAfterLastNameBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), titleAfterLastNamePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"title_after_last_name\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, titleAfterLastNamePrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from titleAfterLastName slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for title_after_last_name")
	}

	if len(titleAfterLastNameAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TitleAfterLastName) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTitleAfterLastName(ctx, exec, o.TitleAfterLastNameID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TitleAfterLastNameSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TitleAfterLastNameSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), titleAfterLastNamePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"title_after_last_name\".* FROM \"title_after_last_name\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, titleAfterLastNamePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in TitleAfterLastNameSlice")
	}

	*o = slice

	return nil
}

// TitleAfterLastNameExists checks if the TitleAfterLastName row exists.
func TitleAfterLastNameExists(ctx context.Context, exec boil.ContextExecutor, titleAfterLastNameID int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"title_after_last_name\" where \"title_after_last_name_id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, titleAfterLastNameID)
	}

	row := exec.QueryRowContext(ctx, sql, titleAfterLastNameID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if title_after_last_name exists")
	}

	return exists, nil
}
